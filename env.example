# MCP Router Configuration

# Port for the router server (default: 4000)
ROUTER_PORT=4000

# Router service name (default: mcp-router)
ROUTER_NAME=mcp-router

# Router service version (default: 1.0.0)
ROUTER_VERSION=1.0.0

# Tool name separator (default: :)
TOOL_NAME_SEPARATOR=:

# Optional: JSON string to override server configurations from servers.json
# Each server should have: name, url, description (optional), enabled (optional)
# MCP_SERVERS='[
#   {
#     "name": "custom-server",
#     "url": "http://localhost:3003/mcp",
#     "description": "My Custom MCP Server",
#     "enabled": true
#   }
# ]'

# Environment mode (development/production)
NODE_ENV=development
